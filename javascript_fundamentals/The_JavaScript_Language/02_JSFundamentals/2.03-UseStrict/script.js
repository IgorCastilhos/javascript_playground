"use strict"

// The directive looks like a string. When it is located at the top of a script, the whole script works the “modern” way.

// Should we "use strict"?
// Modern JavaScript supports "classes" and "modules" that enable use of strict mode automatically.
